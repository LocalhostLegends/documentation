# Users API

## Get All Users

**GET** `/api/users`

Get paginated list of users.

### Query Parameters
- `page`: Page number (default: 1)
- `limit`: Items per page (default: 10)
- `role`: Filter by role